setxkbmap
---------
   setxkbmap tr 
   |
   Klavyeyi Türkçe yapar. Linux'ü her kapattýðýnda deaktif olur.

pwd
---
   Print working directory
   Güncel olarak hangi klasördesin onu gösterir. 

ls
--
   Ýçinde bulunduðumuz klasörün içindeki klasör ve dosyalarý gösterir.

ls -l
-----
   Ýçinde bulunduðumuz klasörün içindeki klasör ve dosyalarý satýr satýr gösterir.

ls -la
------
   Içinde bulundugumuz klasörün içindeki klasör ve dosyalari yine satir satir ama daha çok çiktiyla verir.

cd
--
   cd yazdýktan sonra istediðin klasörün adýný yazýp o klasöre girebilirsin.

cd .. veya sadece cd
--------------------
   Önceki klasöre döner.

mkdir
-----
   Klasör yaratýr. 
   mkdir <klasör ismi>

clear
-----
   Ekraný temizler.

apt-get update
--------------
   Kütüphaneleri vs. çekeceðimiz bütün yerleri günceller.
   Yeni bir þey yüklemek istediðimizde "apt-get" komutunu kullanabilir hale geliriz.

apt-get upgrade
---------------
   Güncelleme yaptýktan sonra yaptýðýn bütün güncellemeleri yükleyip upgrade eder.

apt-get install python-setuptools
apt-get install python3-setuptools
apt-get install dsniff
----------------------------------
   install ile bir þeyler yükleyebiliriz.

yukarý ok
---------
   Önceki komutu tekrar yazar.

adduser
-------
   Kullanýcý ekler.

passwd
------
   Þifreni deðiþtirmek için yeni þifre sorar.

sudo su
-------
   Bu komutu yazýp kendi þifreni verirsen root yetkisini alýrsýn (Eðer zaten root deðilsen.)

ping google.com
---------------
   Ýnternet testi.
   google.com'a bir istek yollar. Bu isteðe cevap alýrsan internet var demektir.

ctrl+c
------
   Ýþlenmekte olan komutu durdurur.

./
--
   .sh dosyalarýný çalýþtýrýr.
   Örnek: ./pycharm.sh

python
------
   <dosya adý>.py dosyalarýný açar.

ifconfig
--------
   IP ve MAC adreslerini gösterir.
   inet yerel IP adresindir. Modem bilgisayarýn kendisidir. Bu modemin içerisinde atanan IP adresleri genelde 10.0.2... gibi gözükür, kendi bilgisayarýmýz da 10.0.2.1'i alýr. Geri kalanlara da atamalarýný yapar.
   ether MAC adresindir.

ifconfig eth0
-------------
   Sadece eth0 kýsmý çalýþýr.

pip install <modül adý>
-----------------------
   No module named '<modül adý>' hatasýnda kullanýlýr.
   VEYA
   sudo apt-get install python-setuptools
   VEYA 
   apt install python-pip
   apt-get install python-pip
   apt-get install python3-pip
   apt-get install python-pip3
   VEYA
   sudo apt-get install software-properties-common
   sudo apt-add-repository universe
   sudo apt-get update
   sudo apt-get install python-pip
   sudo apt-get install python3-pip
   sudo pip install --upgrade pip 
   sudo pip install --upgrade virtualenv
   VEYA
   python -m pip install pynput
   VEYA
   python -m pip   --->pip'i çalistirir
   pip'i güncellemek: pip install --upgrade pip
   
arp -a
------
   Agdaki cihazlarin IP ve MAC adreslerini verir.

kilall python
-------------
   Python ile calisan tum islemleri durdurur.,

cat
---
   Kod içerigini gösterir.

nano
----
   nano projesi açar.
   nano : editör.