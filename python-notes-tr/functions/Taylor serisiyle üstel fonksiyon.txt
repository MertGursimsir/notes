def üstel(x, hata = 1e-8, maxterim = 20):
    n = 1
    sonterim = 1 # serideki ilk terimimiz
    toplam = sonterim

    while sonterim >= hata:
        if n > maxterim :
            print("{} terimde yakýnsama saðlanamadý".format(maxterim))
            break
        sonterim = sonterim * x/n # yeni terim bir öncekini x ile çarpýp terim sýra numarasýna bölerek bulunuyor.
        toplam += sonterim
        n += 1
    return toplam



Bu serinin sonsuz terimi olduðu için bir yerde kesmek zorundayýz. Bu kesme bir hataya yol açacak, ve biz ne kadarlýk bir hataya göz yumabileceðimizi (toleransýmýzý) önceden belirleyeceðiz. Her yeni terim bir öncekinden küçük olduðu için, yeni terim toleranstan küçükse hesabý durduracaðýz.
Ayrýca, kaç tane terim kullanabileceðimizin katý bir sýnýrý da olacak. Belki öyle bir x sayýsý ile baþlarýz ki, seriye ne kadar terim eklersek ekleyelim bir türlü toleransýn altýna inemeyiz. Tabii bu serinin her x deðeri için yakýnsak olduðunu biliyoruz, ama bu ek emniyet tedbiri bu tür iteratif algoritmalarda genelde faydalýdýr. Eðer azami terim sayýsýna ulaþýlmasýna raðmen terimler toleransýn altýna inmediyse ekrana bir uyarý mesajý basýlsýn ve fonksiyon elde ettiði sonucu geri versin.
Tolerans ve azami terim sayýsý için makul varsayýlan deðerler verelim, böylece fonksiyonu kullanýrken her seferinde yazmak zorunda kalmayalým. Sadece deney yapmak istediðimizde deðiþtirelim.